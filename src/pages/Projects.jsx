import usePageTitle from '../usePageTitle.js'

// Placeholder so the "Projects" nav entry from the original site resolves.
// Replace the body with real project content when it is ready.
export default function Projects() {
  usePageTitle('Projects')

  return (
    <div className="container">
      <div className="text-block">
        <h1>Projects</h1>
        <p className="accent">Coming soon</p>
      </div>
    </div>
  )
}
